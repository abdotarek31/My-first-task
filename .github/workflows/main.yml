on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: 'testing github actions'
        run: pwd


      - name: 'printing before'
        run: ls
      
      - uses: actions/checkout@v3
      
      - name: 'printing after'
        run: ls
